
First of all:

(a) Solve trivial issues in 1.1 that don't need refactoring
(b) Refactor the class
(c) Solve the rest of issues in 1.1
(d) Produce 1.2 and make it available in PyPI
(e) Only then start working on 2.0

Ideas for 2.0 most likely on a real Lark parser:

Minimal def example:

swap: pair proj_R proj_L

Larger example:

ant:
    "anterior value, ant(x) = x-1 except ant(0) = 0"
    comp diff (pair id k_1)

sign: comp diff pair id ant

Complete example:

sign:
    "0 for 0, 1 for the rest, sign(x) is then char fct of 'x > 0'"
    comp diff ( pair id ( comp diff (pair id k_1) ) )

